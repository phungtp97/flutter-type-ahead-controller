name: example
description: A new Flutter application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  type_ahead_text_field:
    path: ../
  rxdart:

dev_dependencies:
  flutter_lints:

flutter:

  uses-material-design: true